// See https://aka.ms/new-console-template for more information
Console.WriteLine("Hello, World!");
/*using Uom2023.Data;
using Uom2023.Models;



using Uom2023Context context = new Uom2023Context();

int i = 0;
foreach (UnitsOfMeasure uom in context.UnitsOfMeasures) { 
    Console.WriteLine(uom.Name);
    if (i >= 10)
        break;
    i++;
}

foreach (SameUnit su in context.SameUnits)
{
    Console.WriteLine(su.UomId);
    if (i >= 20)
        break;
    i++;
}

foreach(QuantityType qt in context.QuantityTypes)
{
    Console.WriteLine(qt.QuantityType1);
    if (i >= 30)
        break;
    i++;
}

foreach(ConversionToBaseUnit ctobu in context.ConversionToBaseUnits)
{
    Console.WriteLine(ctobu.UomId);
    if (i >= 40)
        break;
    i++;
}*/